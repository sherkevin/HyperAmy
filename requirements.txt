# HyperAmy 项目依赖
# Python 3.10+ (推荐使用 Python 3.10.18)

# 核心依赖
requests>=2.32.0
python-dotenv>=1.1.0
numpy>=1.26.0
pandas>=2.0.0

# LLM 和 API
openai>=1.91.0
httpx>=0.28.0

# 数据处理
pyarrow>=14.0.0  # 优先使用，如果安装失败则使用 fastparquet
fastparquet>=2025.12.0  # 备用 Parquet 引擎

# 图处理
python-igraph>=0.11.0

# 重试机制
tenacity>=8.5.0

# 其他工具
tqdm>=4.66.0

# 可选依赖（根据需要安装）
# transformers>=4.45.0  # 如果使用 Transformers 模型
# sentence-transformers>=2.2.0  # 如果使用 Transformers embedding
# litellm>=1.73.0  # 如果使用 Bedrock
# torch>=2.0.0  # 如果使用 PyTorch
# vllm>=0.2.0  # 如果使用 VLLM
# gritlm>=1.0.0  # 如果使用 GritLM embedding
# outlines>=0.0.1  # 如果使用 Transformers 离线模式
