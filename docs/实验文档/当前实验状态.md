# 当前实验状态

## 检查时间
2025-12-30 15:30

## 实验进程状态

### 正在运行的进程
- **PID 22558**: 运行中（已运行约2分36秒）
- **PID 22897**: 运行中（刚启动约1分钟）

### 问题
- 有**多个实验进程**在运行，可能重复启动了
- 日志文件为空（0字节），可能输出没有正确写入

## GPU状态
- **所有GPU**: 空闲（0%使用率）
- **显存**: 几乎全部空闲
- **说明**: 实验主要使用API调用，不直接使用GPU

## 结果文件
- **baseline_results.json**: 尚未生成
- **enhanced_results.json**: 尚未生成
- **comparison_results.json**: 尚未生成

## 建议操作

### 1. 清理重复进程
```bash
ssh jiangh@10.103.16.22
cd /media/data4/jiangh/Amygdala/hyperamy_source
# 查看所有实验进程
ps aux | grep test_rigorous_comparison_save | grep -v grep
# 保留最新的，杀掉其他的
kill <旧PID>
```

### 2. 检查日志输出
```bash
# 查看所有日志文件
ls -lh outputs/rigorous_experiment/logs/*.log
# 查看最新日志内容
tail -f outputs/rigorous_experiment/logs/exp_*.log
```

### 3. 重新启动（如果需要）
```bash
# 杀掉所有实验进程
pkill -f test_rigorous_comparison_save
# 重新启动
nohup /media/data4/jiangh/conda/envs/Amygdala/bin/python -m test.test_rigorous_comparison_save > outputs/rigorous_experiment/logs/experiment_$(date +%Y%m%d_%H%M%S).log 2>&1 &
echo $! > outputs/rigorous_experiment/experiment.pid
```

## 预计时间
- **总时间**: 2-4 小时
- **当前状态**: 实验已启动，正在运行中

## 监控命令
```bash
# 检查进程
ssh jiangh@10.103.16.22 "ps aux | grep test_rigorous_comparison_save | grep -v grep"

# 查看日志
ssh jiangh@10.103.16.22 "cd /media/data4/jiangh/Amygdala/hyperamy_source && tail -f outputs/rigorous_experiment/logs/*.log"

# 检查结果
ssh jiangh@10.103.16.22 "cd /media/data4/jiangh/Amygdala/hyperamy_source && ls -lh outputs/rigorous_experiment/*.json"
```
